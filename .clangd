CompileFlags:
  Add: [-std=c++20, -Iinclude, -Wall, -Wextra]
  
# Note: For full Bazel integration, you can generate compile_commands.json manually
# using tools like 'bazel-compile-commands-extractor' or 'hedgehog'
# For now, the include paths above should be sufficient for basic clangd support